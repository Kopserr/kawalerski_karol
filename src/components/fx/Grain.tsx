/** Global film-grain layer — SVG feTurbulence, fixed, overlay-blended.
 * The single cheapest thing that sells "premium" over "wedding-party app"
 * (BRIEF §4.2). Pure CSS, zero JS cost. */
export function Grain() {
  return <div className="grain-overlay" aria-hidden="true" />;
}
